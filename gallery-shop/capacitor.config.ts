import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alamrohmangarden.galleryshop',
  appName: 'gallery-shop',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
